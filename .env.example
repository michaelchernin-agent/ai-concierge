# ===================================================
# AI Concierge — Environment Variables
# ===================================================
# Only ANTHROPIC_API_KEY is required to start.

# --- REQUIRED ---
ANTHROPIC_API_KEY=your_key_here

# --- Server ---
DATA_DIR=./data

# --- Security ---
# Set this to protect dashboard/admin endpoints (leads, config, training)
# Dashboard must send this as X-API-Key header or Bearer token
ADMIN_API_KEY=
# Comma-separated allowed origins for CORS (use * for dev, restrict in production)
ALLOWED_ORIGINS=*

# --- Email (for confirmation emails) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=

# --- WhatsApp Cloud API ---
WHATSAPP_VERIFY_TOKEN=concierge-verify-token
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_AGENT_ID=vamos-events

# --- Instagram Messaging API ---
INSTAGRAM_VERIFY_TOKEN=concierge-verify-token
INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_AGENT_ID=vamos-events

# --- Notifications ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
SLACK_WEBHOOK_URL=
